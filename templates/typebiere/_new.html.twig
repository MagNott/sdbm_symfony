{# {% extends 'base.html.twig' %} #}

{# {% block title %}New Typebiere{% endblock %}

{% block body %} #}
   
    <div class="container w-75 "> <div class="card m-3 shadow bg-light">
		<div class="card-header text-center bg-success-subtle bg-gradient">
			<h5>{% trans %}Ajouter un Type de bière{% endtrans %}</h5>
		</div>

		<div class="card-body">
    {{ include('typebiere/_form.html.twig') }}
    		</div>
	</div>
</div>

    {# <a href="{{ path('app_typebiere_index') }}">back to list</a> #}
{# {% endblock %} #}
