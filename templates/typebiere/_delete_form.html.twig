<form method="post" action="{{ path('app_typebiere_delete', {'idType': typebiere.idType}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typebiere.idType) }}">
    <button class="btn btn-danger btn-sm bi bi-trash3-fill m-1"></button>
</form>
