{% extends 'base.html.twig' %}

{% block title %}Modifier le type de bière{% endblock %}

{% block body %}
    <h1>Modifier le type de bière</h1>
    <div class="container w-75 ">
		<div class="card m-3 shadow bg-light">
			<div class="card-header text-center bg-success-subtle bg-gradient">
				<h1>Modifier le type de bière</h1>
			</div>
			<div class="card-body">

    {{ include('typebiere/_form.html.twig', {'button_label': 'Modifier'}) }}
    <div class="d-flex justify-content-end">

    <a href="{{ path('app_typebiere_index') }}"><button class="btn btn-light">retour à la liste</button>
					</a>
				</div>
			</div>
		</div>
	</div>

    {# {{ include('typebiere/_delete_form.html.twig') }} #}
{% endblock %}
