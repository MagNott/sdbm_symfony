{# {% extends 'base.html.twig' %} #}

{# {% block title %}New Marque{% endblock %}

{% block body %} #}
<div class="container w-75 "> <div class="card m-3 shadow bg-light">
		<div class="card-header text-center bg-success-subtle bg-gradient">
			<h5>{% trans %}Ajouter une Marque{% endtrans %}</h5>
		</div>
        <div class="card-body">
    {{ include('marque/_form.html.twig') }}
    		</div>
	</div>
</div>

    {# <a href="{{ path('app_marque_index') }}">back to list</a> #}
{# {% endblock %} #}
