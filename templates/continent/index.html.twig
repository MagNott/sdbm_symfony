{% extends 'base.html.twig' %}

{% block title %}Continent index{% endblock %}

{% block body %}

<div class="container">
{#Inclusion du formulaire de création dans la vue index#}
{{ include('continent/_new.html.twig', {'button_label': 'Create'}) }}

    <h1>Continent index</h1>

    
    <table class="table">
        <thead>
            <tr>
                <th>Code</th>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for continent in continents %}
            <tr>
                <td>{{ continent.idContinent }}</td>
                <td>{{ continent.nomContinent }}</td>
                <td>
                    {# <a href="{{ path('app_continent_show', {'idContinent': continent.idContinent}) }}">show</a> #}


                    <a href="{{ path('app_continent_edit', {'idContinent': continent.idContinent}) }}"><button class="btn btn-info">Modifier</button></a>

                    {{ include('continent/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    

    {# <a href="{{ path('app_continent_new') }}">Create new</a> #}
    </div>
{% endblock %}
