{% extends 'base.html.twig' %}

{% block title %}Fabricant index{% endblock %}

{% block body %}

<div class="container">
{#Inclusion du formulaire de création dans la vue index#}
{{ include('fabricant/_new.html.twig', {'button_label': 'Create'}) }}


    <h1>Fabricant index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdFabricant</th>
                <th>NomFabricant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fabricant in fabricants %}
            <tr>
                <td>{{ fabricant.idFabricant }}</td>
                <td>{{ fabricant.nomFabricant }}</td>
                <td>
                    {# <a href="{{ path('app_fabricant_show', {'idFabricant': fabricant.idFabricant}) }}">show</a> #}

                    <a href="{{ path('app_fabricant_edit', {'idFabricant': fabricant.idFabricant}) }}"><button class="btn btn-info"> Modifier</button></a>

                     {{ include('fabricant/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('app_fabricant_new') }}">Create new</a> #}
    </div>
{% endblock %}
