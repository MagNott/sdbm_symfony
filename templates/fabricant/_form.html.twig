{{ form_start(form) }}
    {# {{ form_widget(form) }} #}

    <form>
	<div class="d-flex justify-content-center">
		<div class="row">
			<div class="form-group col-md-6">
				{{ form_label(form.nomFabricant) }}
				{{ form_errors(form.nomFabricant) }}
				{{ form_widget(form.nomFabricant) }}
			</div>
            <div class="form-group col-md-6">
				<button class="btn btn-success btn-sm mt-4 ms-5">
					<i class="bi bi-file-earmark-plus me-1"></i>
					{{ button_label|default('Ajouter un Fabricant') | trans }}</button>
			</div>
        </div>
    </div>
{{ form_rest(form) }}
</form>
  
{{ form_end(form) }}
