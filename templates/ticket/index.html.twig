{% extends 'base.html.twig' %}

{% block title %}
	{% trans %}Liste des tickets{% endtrans %}
{% endblock %}

{% block body %}

	<div class='container'>

		{#IMPOSSIBLE DE LE FAIRE FONCTIONNER EN L'ETAT AVEC LA CLEF COMPOSITE - DU COUP JE VIRE LES MODIFS ET LES SUPPRESSIONS, ON FAIT QUE AFFICHER#}
		{#Inclusion du formulaire de création dans la vue index#}
		{# {{ include('ticket/_new.html.twig', {'button_label': 'Create'}) }} #}
			<h2 class="text-center shadow bg-warning-subtle"> {% trans %}Liste des tickets{% endtrans %}</h2>

		<table class="table table-warning table-hover table-striped text-center table-sm shadow-lg table-responsive align-middle">
			<thead>
				<tr>
					<th>{% trans %}Année{% endtrans %}</th>
					<th>{% trans %}Numero de ticket{% endtrans %}</th>
					<th>{% trans %}Date de la vente{% endtrans %}</th>
					{# <th>Actions</th> #}
				</tr>
			</thead>
			<tbody>
				{% for ticket in tickets %}
					<tr>
						<td>{{ ticket.annee }}</td>
						<td>{{ ticket.numeroTicket }}</td>
						<td>{{ ticket.dateVente ? ticket.dateVente|date('Y-m-d H:i:s') : '' }}</td>
						{#<td class="d-flex"> #}
					{# <a href="{{ path('app_ticket_show', {'annee': ticket.annee}) }}">show</a> #}

						{# <a href="{{ path('app_ticket_edit', {'annee': ticket.annee}) }}"><button class="btn btn-info">Modifier</button></a> #}

						{#Ici c'est le bouton du formulaire pour supprimer#}
						{# {{ include('ticket/_delete_form.html.twig') }} #}
						{# </td> #}
					</tr>
				{% else %}
					<tr>
						<td colspan="4">{% trans %}no records found{% endtrans %}</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
	{# <a href="{{ path('app_ticket_new') }}">Create new</a> #}


{% endblock %}
