{% extends 'base.html.twig' %}

{% block title %}{% trans %}Modifier la couleur{% endtrans %}{% endblock %}

{% block body %}
    
    <div class="container w-75 ">
		<div class="card m-3 shadow bg-light">
			<div class="card-header text-center bg-success-subtle bg-gradient">
				<h1>{% trans %}Modifier une couleur{% endtrans %}</h1>
			</div>
			<div class="card-body">

    {{ include('couleur/_form.html.twig', {'button_label': 'Modifier'}) }}
    <div class="d-flex justify-content-end">

    <button class="btn btn-light">{% trans %}retour à la liste{% endtrans %}</button>
    		</div>
			</div>
		</div>
	</div>

    {# {{ include('couleur/_delete_form.html.twig') }} #}
{% endblock %}
