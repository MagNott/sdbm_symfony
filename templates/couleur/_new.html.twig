{# {% extends 'base.html.twig' %}

{% block title %}New Couleur{% endblock %} #}

{# {% block body %} #}
    
    <div class="container w-75 "> <div class="card m-3 shadow bg-light">
		<div class="card-header text-center bg-success-subtle bg-gradient">
			<h5>{% trans %}Ajouter une Couleur{% endtrans %}</h5>
		</div>
      <div class="card-body">
    {{ include('couleur/_form.html.twig') }}
    	</div>
	</div>
</div>

    {# <a href="{{ path('app_couleur_index') }}">back to list</a> #}
{# {% endblock %} #}
